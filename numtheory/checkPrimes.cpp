// one way is to iterate over sqrt(n) numbers of from 6k+-1:- O(sqrt(n))
// other way is to use fermats theorem: to check if p is prime
// take k random numbers from 1 to p-1 and for each check condition of fermats theorem
 // :- O(k*log(n))
 
